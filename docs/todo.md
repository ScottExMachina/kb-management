# ToDos for this Repo

- Add categories
  - ingest
  - fix front matter if it's changed
  - lint update old articles with categories
- update claude.md so global knows it can look here.
  - be able to add multiple kbs
  - kbs have their own section and create it if it doesn't exist.
- move setup, ingest, query, lint to skills
  - setup
    - changes location.  Can set up symlink